
<!--  Send qol referral email -->
<% if @resource.qol_referral %>


	<%= render "qol_referral_invitation", resource: @resource, token: @token %>

<!--  Send physician referral email -->
<% else %>

	<%= render "physician_referral_invitation", resource: @resource, token: @token %>

<% end %>
