<div id="ingredients-container">	
<% @recipe = Recipe.find(params["ingredients_recipe"]["recipe_id"].to_i) %>
	<% @recipe.ingredients_recipes.each do |ingredients_recipe| %>
		<P><strong><%=ingredients_recipe.ingredient.name %></strong>
			<%= link_to "Edit", edit_ingredients_recipe_path(ingredients_recipe, recipe_id: @recipe_id), class: "edit-ingredient", remote: true %>
		</p>
		<p>Amount: <%= ingredients_recipe.amount%> <%= ingredients_recipe.amount_unit%></p>
	<% end %>
</div>