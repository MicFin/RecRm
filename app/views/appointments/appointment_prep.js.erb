


// if showing family in session room then return that save was made

if (Kindrdfood.rooms){

  Kindrdfood.rooms.user_session.minimizeVideo();
  $(".session-tools-container").removeClass("hidden");
  $(".session-tools-container").html("<%= j render(:partial => '/appointments/appointment_prep/show_family', :locals => { :family=> @family }) %>");
  

// else remove and add modal
// may not need this
} 