<div class="container">
<%= simple_form_for(@recipe, html: {class: 'form-horizontal' }) do |recipe_form| %>
  <% if @recipe.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@recipe.errors.count, "error") %> prohibited this recipe from being saved:</h2>

      <ul>
      <% @recipe.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <h3><%= @recipe.name %></h3>
  </div>
  <div class="form-group">
    <%= recipe_form.association :patient_groups, collection: @patient_groups, label: "Select Target Patient Groups",  as: :check_boxes %>
  </div>

  <div class="form-group">
    <%= recipe_form.input :name, :required => true %>
  </div>

  <div class="actions">
    <%= recipe_form.button :submit %>
  </div>
<% end %>
</div>
