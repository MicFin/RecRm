            <div class="survey-item">
              <ul class="list">
                <li><%= link_to "Assessment", appointment_survey_path(surveyable, survey ), remote: true %></li>
                <li>Appointment summary.</li>
                <li>Nutrition analysis.</li>
                <li>Recommended plan.</li>
                <li>&nbsp;</li>
                <li class="text-right"><%= link_to("Edit", edit_appointment_survey_path(surveyable, survey), class: "btn btn-xs btn-primary", remote: true) if current_dietitian %> <%= link_to "View", appointment_survey_path(surveyable, survey), remote: true, class: "btn btn-xs btn-default" %></li>
              </ul>
            </div>