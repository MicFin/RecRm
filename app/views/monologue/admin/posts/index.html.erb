<div class="row">
  <h1>All Content</h1>
  <div class="col-xs-12">
      <table class="table table-striped table-bordered datatable">
        <thead>
          <tr>
            <th><%= t(".title")%></th>
            <th><%= t(".type")%></th>
            <th>Created At</th>
            <th><%= t(".author")%></th>
            <th>sfsadf</th>
            <th></th>
            <th></th>
          </tr>
        </thead>
        <tfoot>
          <tr>
            <th><%= t(".title")%></th>
            <th><%= t(".type")%></th>
            <th>Created At</th>
            <th><%= t(".author")%></th>
            <th>sfsadf</th>
            <th></th>
            <th></th>
          </tr>
        </tfoot>
        <tbody>
          <% @posts.each do |post|%>
            <tr> 
              <td><%= link_to post.title, post.full_url %></td>
              <td><%= post.content_type %></td>
              <td><%= post.user.created_at %> </td>
              <td><span><%= post.user.email %></span> </td>
              <td>sfsadfdsafsdafdsafdsafdsafdsafdasfdsafds dsa fdsa fdsaf dsa dfsafsdafdsfsd</td>

              <td><%= link_to t(".edit"), edit_admin_post_path(post), class: "btn btn-small" %></td>
              <td><%= link_to t(".delete"), [:admin, post],data: {confirm: 'Are you sure?'}, method: :delete, class: "btn btn-small btn-danger" %></td>
            </tr>
          <% end %>
        </tbody>
      </table>
  </div>

</div>







<script type="text/javascript">
  $(document).ready(function() {

    $(".datatable").DataTable({

        // select filter on footer
       initComplete: function () {
            this.api().columns().every( function () {
                var column = this;
                var select = $('<select><option value=""></option></select>')
                    .appendTo( $(column.footer()).empty() )
                    .on( 'change', function () {
                        var val = $.fn.dataTable.util.escapeRegex(
                            $(this).val()
                        );
 
                        column
                            .search( val ? '^'+val+'$' : '', true, false )
                            .draw();
                    } );
 
                column.data().unique().sort().each( function ( d, j ) {
                    select.append( '<option value="'+d+'">'+d+'</option>' )
                } );
            } );
        }
    });
  })
</script>






<% if @page %>
  <%= render partial: "pagination" %>
<% end %>