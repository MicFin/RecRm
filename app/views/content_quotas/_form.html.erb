  <div class="col-xs-6">
<%= simple_form_for(@content_quota) do |f| %>
  <%= f.error_notification %>
  <div class="row">
  <div class="form-inputs col-xs-4">
<%= f.input :dietitian_id, collection: @dietitians, prompt: "Choose a Dietitian", label_method: :email, value_method: :id, :include_hidden => false, :selected => ( [@dietitian.email, @dietitian.id] ), input_html: { class: "form-control"} %>
  <%= f.input :recipes %>
  <%= f.input :quality_reviews %>
  <%= f.input :review_conflicts %>
  </div>
</div>
<div class="row">
  <div class="form-actions col-xs-4">
    <%= f.button :submit %>
  </div>
 </div>
<% end %>
</div>
