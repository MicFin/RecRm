// replace dynamic basic info container with static basic info show page
$("#basic-info-container").replaceWith("<%= j render(:partial => 'basic_info_show') %>");
// replace basic info for with add ingrdients form
$("#basic-info-form").replaceWith("<%= j render(:partial => 'add_ingredients_form') %>");
// hide next button 
$(".hidden-form").hide();
// 
$.ajax({
           type: "GET",
           datatype: "html",
           url: "/ingredients_recipes/new",
           data: {recipe_id:"<%=@recipe_id%>"},
           success: function(response){
           } 
       });
// update breadcrumb with recipe id
$("#recipe-breadcrumb").replaceWith("<%= j render(:partial => 'recipes/new_recipe_breadcrumbs') %>");
// update breadcrumb ingredients now enabled
$("#breadcrumb-ingredients").removeClass("disabled");
$("#breadcrumb-ingredients").tooltip("disable");
$("#breadcrumb-ingredients a").on("click", function(e){
	return true;
});