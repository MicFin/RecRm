
// if showing family in session room then minimize video
if (Kindrdfood.rooms){

	// minimize video
	Kindrdfood.rooms.user_session.minimizeVideo();

	// update navigation
  Kindrdfood.rooms.user_session.updateNavigation("session-nav-notes");

	// show container and load partial
	Kindrdfood.ajaxSpecific.ajaxUpdateUrl.showPagePartial("<%= j render(:partial => '/appointments/appointment_prep/survey_form', :locals => { :survey=> @survey, :surveyable => @surveyable}) %>");

} 