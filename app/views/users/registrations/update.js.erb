
// if showing family in session room then minimize video
if (Kindrdfood.rooms){

	Kindrdfood.rooms.user_session.minimizeVideo();

}

// show container and load partial
Kindrdfood.ajaxSpecific.ajaxUpdateUrl.showPagePartial("<%= j render(:partial => '/families/list_family_members', :locals => { :family=> @family, :user => @user}) %>");

