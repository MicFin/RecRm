


// if showing family in a room then

if (Kindrdfood.rooms){ // checks if Kindrdfood.rooms controller specific JS is loaded

  // minimize video
  Kindrdfood.rooms.user_session.minimizeVideo();

   // update navigation
  Kindrdfood.rooms.user_session.updateNavigation("session-nav-appointment-prep");

	// show container and load partial
	Kindrdfood.rooms.user_session.showToolPartial("<%= j render(:partial => '/appointments/appointment_prep/show_family', :locals => { :family=> @family }) %>");


} 