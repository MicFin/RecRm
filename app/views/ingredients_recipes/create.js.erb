
// update ingredients recipe list
$("#ingredients-container").replaceWith("<%= j render(:partial => 'ingredients_recipe_list') %>");
// show next button
$("#breadcrumb-steps").removeClass("disabled");
$("#breadcrumb-steps").tooltip("disable");
$("#breadcrumb-steps").on("click", function(){
  return true;
});

$(".hidden-form").show();
// set new ingredient form
$.ajax({
   type: "GET",
   datatype: "script",
   url: "/ingredients_recipes/new",
   data: {recipe_id:"<%=@recipe_id%>"},
   success: function(response){
    // could alert that ingrdient was created
   } 
});
// set next button from hidden form
// if next button is clicked then go to new step
$(".next-button").on("click", function(e){
  e.preventDefault;
  // recplace ingredient form with add new steps form
  $("#new-ingredient-form").replaceWith("<%= j render(:partial => '/recipes/add_steps_form') %>"); 
  $(".hidden-form").hide();
  $.ajax({
     type: "GET",
     datatype: "script",
     url: "/recipe_steps/new",
     data: {recipe_id:"<%=@recipe_id%>"},
     success: function(response){
      // could alert that all ingredients are saved  and now do steps
     } 
  });
});
