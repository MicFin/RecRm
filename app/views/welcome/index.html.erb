<div id="dashboard-container" class="">
<!-- 				<p><%= link_to image_tag("pencil.png", class: "grow"), new_article_path %>	</p>
				<p>Write an Article</p>
 -->

 	<div class="form-group col-xs-12 form-group text-center">
 		<h2>Welcome back to the kitchen, <%=current_dietitian.email %>!</h2>
 	</div>
	<div class="col-xs-12 col-sm-5 col-md-5 col-lg-5 text-center">
		<p><%= link_to image_tag("chef-hat.png", class: "tilt"), :controller => "recipes", :action => "new" %>	</p>
		<p>Create a Recipe</p>
	</div>
	<div id="appointment-update-container" class="col-xs-12 col-sm-2 col-md-2 col-lg-2 text-center alert alert-danger">
		<p>Your upcoming sessions</p>
		<% @appointments.each do |appointment| %>
		<h4><%#=@appointment.in_time_zone("Eastern Time (US & Canada)").strftime("%B %d, %Y %I:%M %p"%></h4>
		<% if appointment.room_id %>
		<%# if (((DateTime.now.in_time_zone("Eastern Time (US & Canada)") - @appointment.start_time) / 60 / 60) > -5)%>
			<%= link_to 'Join Session', {:controller => "rooms", :action => "in_session", :id => appointment.room_id, appointment: appointment.id }, class: "text-center action kindrd-button btn-block tilt"%>
		<% else %>
			<h5>Return 2 hours before appointment to sign in</h5>
		<% end %>
		<% end %>
	</div>
	<div class="col-xs-12 col-sm-5 col-md-5 col-lg-5 text-center">
		<p><%= link_to image_tag("computer.png", class: "tilt"), "http://www.kindrdcare.com/sign_in" %></p>
		<p>See a Client</p>
	</div>
</div>

<div id="dietitian-recipes-container" class="container">

	<div class="col-xs-3">
	
	</div>
</div>