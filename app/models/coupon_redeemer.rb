class CouponRedeemer < ActiveRecord::Base

  def self.redeem_coupon(appointment, user, coupon)
    
    # check if coupon is still valid

    # check if appointment has a coupon already applied

    # 
  end
end
