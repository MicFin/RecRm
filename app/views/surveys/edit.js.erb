
// if showing family in session room then minimize video
if (Kindrdfood.rooms){

	// minimize video
	Kindrdfood.rooms.user_session.minimizeVideo();

	// update navigation
  Kindrdfood.rooms.user_session.updateNavigation("session-nav-notes");

	// show container and load partial
	Kindrdfood.ajaxSpecific.ajaxUpdateUrl.showPagePartial("<%= j render(:partial => '/surveys/form', :locals => { :survey=> @survey, :surveyable => @surveyable, remote_boolean: true, survey_group: @survey_group}) %>");

	// initiate datetimepicker
	Kindrdfood.dateTimePickers.dateTimePicker.init();

	// initiate datepicker
	Kindrdfood.dateTimePickers.dateTimePicker.dateOnlyInit();

 // // KRDN prep in session JS
 //  // minimize video
 //  Kindrdfood.rooms.user_session.minimizeVideo();

 //   // update navigation
 //  Kindrdfood.rooms.user_session.updateNavigation("session-nav-appointment-prep");

	// // show container and load partial
	// Kindrdfood.ajaxSpecific.ajaxUpdateUrl.showPagePartial("<%#= j render(:partial => '/families/show', :locals => { :family=> @family }) %>");



// not sure how to handle the js yet
// } else if ( <%#= @survey.survey_group.name == "Client Pre-Appointment" %>) {


// 	// validate form
// 	Kindrdfood.formValidations.growthEntries.clientForm();
// 	Kindrdfood.formValidations.foodDiaryEntries.init();
// 	Kindrdfood.formValidations.surveys.openResponse();

// 	// initiate carousel
// 	Kindrdfood.bootstrapOverrides.carousel.init();
// 	// initiate datetimepicker
// 	Kindrdfood.dateTimePickers.dateTimePicker.init();
} 