<% content_for :title do %>
  <%= @post.title %>
<% end %>

<% content_for :meta_description do %>
  <%= truncate(strip_tags(@post.content), length: 155) %>
<% end %>


<div class="landing-page education-page">

  <%= render "monologue/posts/posts_nav" %>
  <div class="landing-page-body">
    <section id="alerts-container">
      <%= render "/layouts/alerts" %>
    </section>
    <section class="splash-image-section">
      <div class="left-splash-column">
        <h1>Articles and Information</h1>
      </div>
    </section>

    <section class="articles-container">
      <article>
        <%= render partial: 'monologue/posts/post_header', locals: {post: @post }%>
        <div class="content" data-monologue="content">
          <%= raw @post.content %>
        </div>

        <div class="tags" data-monologue="tags">
          <%#= render @post.tags %>
        </div>

        <%#= render "monologue/posts/social_sharing" %>

        <!--       <div id="disqus_thread"></div>
        <%#= render 'layouts/monologue/application/disqus_embed' %>
        <noscript>Please enable JavaScript to view the comments.</noscript> -->
      </article>
      
    </section>
    <section class="articles-side-bar hidden-xs hidden-sm" data-spy="affix" data-offset-top="200">
<!--       <nav class="articles-nav">
        <h3>Popular Recipes</h3>
        <ul>
          <%# @tags.each do |tag| %>
            <li id="tag"><%#=link_to "#{tag.name} (#{tag.frequency})", tag_url(tag) %> </li>
          <%# end %>
        </ul>
        <h3>Popular Articles</h3>
        <ul>
          <%# @tags.each do |tag| %>
            <li id="tag"><%#=link_to "#{tag.name} (#{tag.frequency})", tag_url(tag) %> </li>
          <%# end %>
        </ul>
        <p>Kindrdfood recipes and articles are created by a team of Registered Dietitians and Chefs to meet the needs of each Kindrdfood family.</p>
      </nav> -->
      <aside class="call-to-action-container">
        <%= link_to "Get Custom Recipes", main_app.landing_pages_index_path, class: "btn btn-lg btn-primary"%>
       <!--  <button class="btn btn-block btn-primary">Get Custom Recipes Now!</button> -->
      </aside>
    </section>
    <%= render "monologue/posts/posts_footer" %>
  </div>
</div>