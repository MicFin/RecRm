

// remove existing modal
$("#appointmentPrepModal").remove();

// add modal with updated data
$("body").append("<%= j render(:partial => '/appointments/prep_modal', :locals => { appointment: @appointment}) %>");

// activate modal
$("#appointmentPrepModal").modal({
						backdrop: 'static',
            keyboard: false
});
