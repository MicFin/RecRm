    
    // if showing family in session room then return that save was made
    if (Kindrdfood.rooms){

      Kindrdfood.rooms.user_session.minimizeVideo();
      $(".session-tools-container").removeClass("hidden");
      $(".session-tools-container").html("<%= j render(:partial => '/surveys/survey_answers', :locals => { :survey=> @survey, :surveyable => @surveyable}) %>");
      

    // else hide modal and adjust button
    } 
